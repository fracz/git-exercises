When you have added too many changes to staging area, you can undo them with [`git reset <file>`](https://git-scm.com/docs/git-reset) command
before the [`git commit`](https://git-scm.com/docs/git-commit).
